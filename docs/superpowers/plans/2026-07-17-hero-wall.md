# Hero 铺满式 3D 卡片墙 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Hero 第一屏的 `Orbit` 轨道装置换成 Linear intake 式的铺满 3D 卡片墙；前置砍掉浅色主题。

**Architecture:** 先做减法（浅色下线，Task 1–2），再做加法（数据结构 → 墙组件 → Hero 重排 → 移动端 → 性能，Task 3–7）。
墙是一面 `rotateX(42deg)` 的 3D 平面，五行卡片以不同速度无限漂移，靠**毛玻璃模糊**（而非压暗）保住文案可读性。

**Tech Stack:** Next.js 15 (App Router) · React 19 · TypeScript · Tailwind v4 · motion v12 · bun

**Spec:** `docs/superpowers/specs/2026-07-17-hero-wall-design.md` — 本计划的每个数值都出自那里，有冲突以 spec 为准。

## Global Constraints

- **包管理器是 `bun`**，不是 npm/yarn/pnpm。所有命令用 `bun run <script>`。
- **每个 Task 收尾必须跑通** `bun run typecheck`（预期无输出）和 `bun run build`（预期 `Compiled successfully`）。任一失败则该 Task 未完成。
- **本项目没有测试框架，且本次不引入。** 删除类任务用 `grep` 断言验证，视觉类任务用无头截图 + 人眼验证。详见每个 Task 的验证步骤。
- **所有可见文字必须双语**，走 `lib/content.ts` 的 `Copy = { zh, en }` 约定，不在组件里硬编码中文。
- **`lib/useRelease.ts` 全程不许改。** 它含 `inflight` 变量，会被 `grep light` 误伤。它管下载链路——页面上唯一真正赚钱的东西。
- **下载按钮三态数据链路**（loading / 直链 / 降级到 Releases）全程不许改，`Hero.tsx` 里那段 `getPlatformCards` / `guessPlatform` 逻辑原样保留。
- **hydration 门闩**：任何依赖 `window` / `navigator` / 指针位置的值，必须等 `mounted` 之后再接。服务器上没有这些，首帧对不上会报 hydration 不匹配（项目已踩过两次）。
- **墙是背景不是内容**：容器必须 `aria-hidden="true"` + `pointer-events:none`。
- **`prefers-reduced-motion: reduce`** 下所有动画停止（墙静止但仍可见）。
- 提交信息用中文，遵循现有风格（`feat:` / `docs:` / `refactor:` 前缀）。
- **截图前必须先清掉旧的 dev server**，否则端口被占，Next 会静悄悄跳到 3001，
  而截图仍去访问 3000 —— 拍到上一个任务的旧页面。**这种错不报错，只给你一张看似正常的旧图。**
  每个截图步骤一律用这个固定开头：

  ```bash
  cd /Users/kika/Desktop/project/Electron/claude-desktop-website
  pkill -f "next dev" 2>/dev/null; sleep 1
  bun run dev > /dev/null 2>&1 &
  sleep 5
  curl -sf http://localhost:3000 > /dev/null && echo "dev server 就绪（3000）" || echo "启动失败——不要相信后面的截图"
  ```

  必须看到 `dev server 就绪（3000）` 才继续截图。截完 `pkill -f "next dev"`。
- **截图一律存到 scratchpad**，不要污染项目目录：
  `/private/tmp/claude-501/-Users-kika-Desktop-project-Electron-claude-desktop-website/9014c5d1-5a6f-4f78-86c9-99e53f1efb43/scratchpad/`
- 每个 Chrome 无头截图用**独立的** `--user-data-dir`（如 `/tmp/cdp-t1`、`/tmp/cdp-t4`），
  否则会报 `browser is already running` 而拿不到图。

## File Structure

| 文件 | 责任 | 本计划中的动作 |
|---|---|---|
| `app/globals.css` | 设计令牌 + 全局动画 keyframes | Task 1 删 `.light` 与 `--wall-opacity`；Task 4 换墙的 keyframes；Task 2 删 orbit 系列 |
| `lib/prefs.tsx` | 访客偏好（改后只剩语言） | Task 1 删主题相关全部 |
| `components/Nav.tsx` | 顶部导航 | Task 1 删主题切换按钮 |
| `app/layout.tsx` | 根布局 | Task 1 删防闪白内联 script |
| `components/fx/Ambient.tsx` | 背景氛围粒子 | Task 1 主题分支 → 写死深色 |
| `lib/content.ts` | 全站文案与数据（双语） | Task 1 删 `ui.themeToggle`；Task 3 升级 `heroWall` 数据结构 |
| `components/Orbit.tsx` | 旧轨道装置 | Task 2 **删除** |
| `components/HeroWall.tsx` | **墙**：只管画墙，不知道 Hero 里有什么 | Task 4 重写 |
| `components/sections/Hero.tsx` | 第一屏排版与文案 | Task 5 双栏→单栏、移出 Terminal |
| `app/page.tsx` | 页面装配 | Task 5 Terminal 升为独立 section |

边界纪律：`HeroWall` 不接受任何来自 Hero 的布局参数，Hero 也不知道墙怎么动。两者唯一的耦合是「墙在 Hero 里被摆在 `absolute inset-0`」。

---

## Task 1: 浅色主题下线

**Files:**（行号已核实）
- Modify: `app/globals.css:61-80`（`.light` 块）、`:180-185`（`--wall-opacity` 整块）、`:4-20`（顶部注释）
- Modify: `lib/prefs.tsx`（全文重写，只留语言）
- Modify: `components/Nav.tsx:17`（解构）、`:43-49`（按钮）、`:82-96`（两个图标函数）
- Modify: `app/layout.tsx:4,69`
- Modify: `components/fx/Ambient.tsx:70,74`
- Modify: `lib/content.ts:342`

**Interfaces:**
- Consumes: 无（第一个 Task）
- Produces: `usePrefs()` 的返回类型收窄为 `{ lang, setLang, t }`——**`theme` 和 `toggleTheme` 不再存在**。后续所有 Task 都不许再引用它们。`themeInitScript` 导出被删除。

- [ ] **Step 1: 先记录基线——确认改之前 grep 能抓到什么**

```bash
cd /Users/kika/Desktop/project/Electron/claude-desktop-website
grep -rn "toggleTheme\|themeInitScript\|wall-opacity" app components lib | grep -v node_modules
```

Expected: 抓到若干行（`Nav.tsx` 的 toggleTheme、`layout.tsx` 的 themeInitScript、`prefs.tsx` 的定义、`HeroWall.tsx` 的 wall-opacity 等）。
**这就是本 Task 的「失败的测试」**：改完之后同一条命令必须一无所获。

- [ ] **Step 2: 重写 `lib/prefs.tsx`——只留语言**

整个文件替换成：

```tsx
'use client'

/*
  ─────────────────────────────────────────────────────────────
  访客偏好：只剩语言（中/EN）。
  ─────────────────────────────────────────────────────────────
  ('use client' = 这个文件里的代码要在访客浏览器里跑，不是在服务器上。
   凡是要读 localStorage、监听点击、用 useState 的地方都得标它。)

  主题在 2026-07-17 下线：产品拍板只做深色。深色值直接写在 globals.css
  的 :root 里，不挂任何类就成立——所以既不需要主题状态，也不需要
  防闪白脚本（那段脚本以前的活儿是「首帧前判断该不该挂浅色」，
  现在没有浅色可挂了）。想找回浅色去翻 git 历史。

  语言默认策略不变：先看存档，再猜浏览器语言，兜底中文（主要受众）。
*/

import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'
import type { Copy, Lang } from './content'

type Prefs = {
  lang: Lang
  setLang: (l: Lang) => void
  /** 取一句双语文案里当前语言那一半。组件里到处用它。 */
  t: (c: Copy) => string
}

const PrefsContext = createContext<Prefs | null>(null)

const LANG_KEY = 'cd-lang'

export function PrefsProvider({ children }: { children: ReactNode }) {
  /*
    初值必须和服务器渲染出来的 HTML 一致，否则 React 报 hydration 不匹配
    （两边第一帧长得不一样）。所以先用默认值 zh，真实偏好等挂载后在
    useEffect 里读——那时已经在浏览器里了。
  */
  const [lang, setLangState] = useState<Lang>('zh')

  useEffect(() => {
    const savedLang = localStorage.getItem(LANG_KEY) as Lang | null
    if (savedLang === 'zh' || savedLang === 'en') {
      setLangState(savedLang)
    } else {
      const nav = navigator.language ?? ''
      setLangState(nav && !nav.toLowerCase().startsWith('zh') ? 'en' : 'zh')
    }
  }, [])

  // lang 属性跟着改：屏幕阅读器靠它决定用中文还是英文发音。
  useEffect(() => {
    document.documentElement.lang = lang === 'zh' ? 'zh-CN' : 'en'
  }, [lang])

  const setLang = (l: Lang) => {
    setLangState(l)
    try {
      localStorage.setItem(LANG_KEY, l)
    } catch {
      // 隐私模式下存不了。本次会话内照样能切，刷新后不记得——可以接受。
    }
  }

  const t = (c: Copy) => c[lang]

  return <PrefsContext.Provider value={{ lang, setLang, t }}>{children}</PrefsContext.Provider>
}

export function usePrefs(): Prefs {
  const ctx = useContext(PrefsContext)
  // 忘了套 Provider 是写代码时的错，不是运行时的意外——直接报清楚。
  if (!ctx) throw new Error('usePrefs 必须在 <PrefsProvider> 内部使用')
  return ctx
}
```

**注意**：老访客 localStorage 里残留的 `cd-theme` 键**不要写代码清理**。脚本和 `.light` 都没了，那条记录成了没人听的话，无害。为它写迁移是过度设计。

- [ ] **Step 3: `app/layout.tsx` 删防闪白脚本**

改 `:4` 的 import：

```tsx
import { PrefsProvider } from '@/lib/prefs'
```

删掉 `:69` 整行：

```tsx
<script dangerouslySetInnerHTML={{ __html: themeInitScript }} />
```

（顺带收益：少一个阻塞首帧的内联脚本。）

- [ ] **Step 4: `components/Nav.tsx` 删切换按钮**

`:17` 改成：

```tsx
const { t, lang, setLang } = usePrefs()
```

删掉 `:43-49` 那个 `<button onClick={toggleTheme}>` 整块（含 `aria-label={t(ui.themeToggle)}` 和 `{theme === 'dark' ? <SunIcon /> : <MoonIcon />}`）。
删掉 `:82-89` 的 `function SunIcon()` 和 `:91-96` 的 `function MoonIcon()`。

**保留** `:42` 的 `<div className="ml-auto flex items-center gap-1">` 容器——语言按钮、GitHub、下载还在里面。
它用 `gap-1` 排列，少一个子元素不会留空洞，但仍需 Step 10 截图确认。

- [ ] **Step 5: `components/fx/Ambient.tsx` 写死深色**

`:70` 删掉 `const { theme } = usePrefs()` 这行（如果 `usePrefs` 在该文件再无其他用途，把 import 也删掉）。
`:74` 改成：

```tsx
// 主题在 2026-07-17 下线，只剩深色——不再有分支
color.current = { fill: '#4ade80', boost: 1 }
```

- [ ] **Step 6: `lib/content.ts` 删文案**

删掉 `:342` 的 `themeToggle: { zh: '切换深浅色', en: 'Toggle theme' },`。

- [ ] **Step 7: `app/globals.css` 删浅色层与孤儿令牌**

1. 删掉 `:61-80` 整个 `.light { ... }` 块。
2. 删掉 **`:180-185` 整块**——即：

```css
:root {
  --wall-opacity: 0.5; /* 深色：墙隐约可见 */
}
.light {
  --wall-opacity: 0.42; /* 浅色：白底噪声容忍度更低，再压一档 */
}
```

  这个 `:root` 里**只有** `--wall-opacity` 一个令牌，删了令牌整块就空了——`:root {}` 留着是垃圾，一起删。
  该令牌整体作废（新墙每行独立控透明度，见 Task 4），不是只删它的浅色覆盖。
  **注意**：紧邻上方 `:175-179` 的 `@keyframes wallDrift` **必须保留**，Task 4 还要用。

3. `:38` 那个 `:root`（真正的设计令牌块）**原样保留**，含 `color-scheme: dark;`（它告诉浏览器滚动条、表单控件用深色渲染）。
4. 把 `:4-20` 的顶部注释改成：

```css
/*
  ─────────────────────────────────────────────────────────────
  设计令牌——「发布会」版（设计稿 D 定稿，2026-07-17）。
  ─────────────────────────────────────────────────────────────
  **只有深色。** 浅色主题于 2026-07-17 下线（产品拍板），
  连同切换按钮、.light 覆盖层、防闪白脚本一起删除——留着没人维护的
  死代码是负债，需要时去翻 git 历史。

  深色值直接写在 :root，不挂任何类就成立，JS 加载前也成立，首帧不闪白。

  @theme inline 的 --color-* 会被 Tailwind 编成工具类（bg-canvas / text-brand…）。
  颜色一律走 var() 间接引用——正因为有这层间接，砍掉整个浅色主题只需要删
  两个 CSS 块，而不用去 20 个组件里逐个改颜色。这层别拆。
*/
```

- [ ] **Step 8: 验证——grep 必须一无所获**

```bash
cd /Users/kika/Desktop/project/Electron/claude-desktop-website
echo "── 应当无输出 ──"
grep -rn "toggleTheme\|themeInitScript\|wall-opacity\|ui.themeToggle\|SunIcon\|MoonIcon" app components lib
grep -rn "^\.light\|\.light {" app/globals.css
echo "── useRelease.ts 必须完好（含 inflight，勿被误伤）──"
grep -c "inflight" lib/useRelease.ts
```

Expected: 前两条 grep **无任何输出**（Step 1 抓到的东西现在全没了）；最后一条输出 `3`（`useRelease.ts` 未被动过）。

- [ ] **Step 9: typecheck + build**

```bash
bun run typecheck && bun run build
```

Expected: typecheck 无输出；build 打印 `Compiled successfully`。
**若报 `Property 'theme' does not exist`**，说明还有组件在用旧的 `usePrefs()` 返回值——按报错位置删掉对应引用。

- [ ] **Step 10: 人眼验证——截图看 Nav 没留空洞**

```bash
bun run dev &
sleep 4
cd /private/tmp/claude-501/-Users-kika-Desktop-project-Electron-claude-desktop-website/9014c5d1-5a6f-4f78-86c9-99e53f1efb43/scratchpad
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --screenshot=t1-nav.png --window-size=1440,900 --virtual-time-budget=3000 \
  --user-data-dir=/tmp/cdp-t1 http://localhost:3000
```

Expected: 打开 `t1-nav.png`，Nav 上只剩「EN / GitHub / 下载」，**没有太阳图标**，按钮间距均匀无空洞。

- [ ] **Step 11: 提交**

```bash
git add -A app components lib
git commit -m "refactor: 浅色主题下线，只保留深色

删除 .light 覆盖层、主题切换按钮、防闪白内联脚本与 theme 状态。
连带删除孤儿令牌 --wall-opacity（新墙改为每行独立控透明度）。
颜色的 var() 间接层原样保留——正是它让这次删除只动了两个 CSS 块。"
```

---

## Task 2: Orbit 退役

**Files:**
- Delete: `components/Orbit.tsx`
- Modify: `components/sections/Hero.tsx:18,144`（删 import 与 `<Orbit />`）
- Modify: `app/globals.css`（删 orbit 相关 keyframes 与类）

**Interfaces:**
- Consumes: Task 1 的 `usePrefs()`（已无 theme）
- Produces: `Orbit` 组件不复存在。`Hero.tsx` 的右栏暂时空着——Task 5 才重排版面。本 Task 结束时页面右侧是空的，**这是预期状态**。

- [ ] **Step 1: 先摸清 Orbit 的 CSS 足迹**

```bash
cd /Users/kika/Desktop/project/Electron/claude-desktop-website
grep -n "orbitOuter\|orbitInner\|sat-outer\|sat-inner\|pulse-ring\|orbit-rings" app/globals.css
```

Expected: 抓到若干行（keyframes 定义 + 类定义）。记下**全部**行号——Step 3 要一个不留地删掉。

- [ ] **Step 2: 删组件文件与引用**

```bash
rm components/Orbit.tsx
```

`components/sections/Hero.tsx`：删掉 `:18` 的 `import { Orbit } from '../Orbit'`，删掉 `:144` 的 `<Orbit />`。

- [ ] **Step 3: 删 Orbit 的全部 CSS**

按 Step 1 抓到的行号，从 `app/globals.css` 删掉：
`@keyframes orbitOuter`、`@keyframes orbitInner`、`.sat-outer`、`.sat-inner`、`.pulse-ring`（含它的 `@keyframes`）、`.orbit-rings`（含它的 `@keyframes`）。

**判断标准**：一个 keyframes/类，只要只有 Orbit 在用，就删。留着没人用的动画是死代码。

- [ ] **Step 4: 验证——grep 必须一无所获**

```bash
echo "── 应当无输出 ──"
grep -rn "orbitOuter\|orbitInner\|sat-outer\|sat-inner\|pulse-ring\|orbit-rings\|from '../Orbit'\|<Orbit" app components lib
ls components/Orbit.tsx 2>&1
```

Expected: grep 无输出；`ls` 报 `No such file or directory`。

- [ ] **Step 5: typecheck + build**

```bash
bun run typecheck && bun run build
```

Expected: typecheck 无输出；build 打印 `Compiled successfully`。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "refactor: Orbit 轨道装置退役

轨道的圆周运动和 HeroWall 的 3D 透视是两种纵深语言，叠在一起眼睛
不知道该把哪个当远处（同类教训见 HeroWall 注释里退役的四根光束）。
组件、CSS keyframes、类定义一并删除，不留死代码。
右栏暂时空着，Hero 版面由后续任务重排。"
```

---

## Task 3: heroWall 数据结构升级

**Files:**
- Modify: `lib/content.ts:80-122`（`WallCard` 类型 + `heroWall` 数据）

**Interfaces:**
- Consumes: `Copy` / `I18n` 类型（`lib/content.ts` 既有）
- Produces: 供 Task 4 消费——
  ```ts
  export type WallCard = {
    id: Copy       // 顶行状态
    title: Copy    // 主行
    tag: Copy      // 底部标签
    brand?: boolean
    ask?: boolean
  }
  export const heroWall: WallCard[][]   // 5 行 × 5 张
  ```
  **字段名就是这几个**，Task 4 直接按这个名字读，别改。

- [ ] **Step 1: 确认现有的 Copy 类型名**

```bash
grep -n "export type Copy\|type Copy\|export type Lang" lib/content.ts
```

Expected: 找到 `Copy` 的定义（形如 `{ zh: string; en: string }`）。
**下一步的类型定义必须复用它**，不要新造一个 `I18n`。

- [ ] **Step 2: 替换 `WallCard` 类型与 `heroWall` 数据**

把 `lib/content.ts` 里现有的 `WallCard` 类型定义和整个 `heroWall` 数组替换成：

```ts
/*
  Hero 卡片墙的内容。文案是「产品干活的真实痕迹」——
  每行必有一张 ask 卡（用户说的话），它是这面墙的叙事锚点：
  先有人说一句话，然后才有那一串产出。别把 ask 卡删光，会散架。
*/
export type WallCard = {
  /** 顶行：会话号 / 产出体积 / 耗时 */
  id: Copy
  /** 主行：指令原话，或产出文件名 */
  title: Copy
  /** 底部标签：技能名 / 扩展名 */
  tag: Copy
  /** 标签用品牌绿 */
  brand?: boolean
  /** 用户指令卡（每行一张，描边和背景都不同） */
  ask?: boolean
}

export const heroWall: WallCard[][] = [
  [
    { id: { zh: 'ENG · 会话 #12', en: 'ENG · Session #12' }, title: { zh: '帮我做一份 Q3 复盘 PPT', en: 'Make me a Q3 review deck' }, tag: { zh: 'ppt-master', en: 'ppt-master' }, brand: true, ask: true },
    { id: { zh: '已完成 · 2 分 14 秒', en: 'Done · 2m 14s' }, title: { zh: 'ppt-master · 生成 24 页', en: 'ppt-master · 24 slides' }, tag: { zh: '技能', en: 'Skill' } },
    { id: { zh: '产出 · 4.2 MB', en: 'Output · 4.2 MB' }, title: { zh: 'Q3-复盘.pptx', en: 'Q3-review.pptx' }, tag: { zh: '.pptx', en: '.pptx' } },
    { id: { zh: '画布 · 3 处修改', en: 'Canvas · 3 edits' }, title: { zh: '画布已更新', en: 'Canvas updated' }, tag: { zh: 'canvas', en: 'canvas' }, brand: true },
    { id: { zh: '已完成', en: 'Done' }, title: { zh: 'd3-visualization · 图表已嵌入', en: 'd3-visualization · charts embedded' }, tag: { zh: '技能', en: 'Skill' } },
  ],
  [
    { id: { zh: '知识库 · 12 条命中', en: 'Knowledge base · 12 hits' }, title: { zh: '检索本地知识库', en: 'Searching local knowledge' }, tag: { zh: 'RAG', en: 'RAG' } },
    { id: { zh: 'ENG · 会话 #12', en: 'ENG · Session #12' }, title: { zh: '把这份表格算个季度汇总', en: 'Summarize this sheet by quarter' }, tag: { zh: 'spreadsheets', en: 'spreadsheets' }, brand: true, ask: true },
    { id: { zh: '已完成', en: 'Done' }, title: { zh: 'spreadsheets · 公式已写入', en: 'spreadsheets · formulas written' }, tag: { zh: '技能', en: 'Skill' } },
    { id: { zh: '产出 · 890 KB', en: 'Output · 890 KB' }, title: { zh: '年度预算表.xlsx', en: 'annual-budget.xlsx' }, tag: { zh: '.xlsx', en: '.xlsx' } },
    { id: { zh: '等待确认', en: 'Awaiting approval' }, title: { zh: '请求运行命令', en: 'Command request' }, tag: { zh: '权限', en: 'Permission' } },
  ],
  [
    { id: { zh: '已完成 · 出图 4 张', en: 'Done · 4 images' }, title: { zh: 'imagegen · 生成完毕', en: 'imagegen · complete' }, tag: { zh: '技能', en: 'Skill' } },
    { id: { zh: '产出 · 1.1 MB', en: 'Output · 1.1 MB' }, title: { zh: '发布海报.png', en: 'launch-poster.png' }, tag: { zh: '.png', en: '.png' } },
    { id: { zh: 'ENG · 会话 #13', en: 'ENG · Session #13' }, title: { zh: '写一版融资方案的框架', en: 'Draft a fundraising proposal outline' }, tag: { zh: 'proposal-writer', en: 'proposal-writer' }, brand: true, ask: true },
    { id: { zh: '已完成', en: 'Done' }, title: { zh: 'proposal-writer · 框架已搭好', en: 'proposal-writer · outline ready' }, tag: { zh: '技能', en: 'Skill' } },
    { id: { zh: '产出 · 36 KB', en: 'Output · 36 KB' }, title: { zh: '投标方案.docx', en: 'proposal.docx' }, tag: { zh: '.docx', en: '.docx' } },
  ],
  [
    { id: { zh: '运行中 · 3 个任务', en: 'Running · 3 tasks' }, title: { zh: '会话 #14', en: 'Session #14' }, tag: { zh: '进行中', en: 'Active' }, brand: true },
    { id: { zh: '渲染完成', en: 'Render complete' }, title: { zh: 'sora · 成片已导出', en: 'sora · export done' }, tag: { zh: '技能', en: 'Skill' } },
    { id: { zh: '产出 · 48 MB', en: 'Output · 48 MB' }, title: { zh: '产品演示.mp4', en: 'product-demo.mp4' }, tag: { zh: '.mp4', en: '.mp4' } },
    { id: { zh: 'ENG · 会话 #14', en: 'ENG · Session #14' }, title: { zh: '把这些数据做成可交互看板', en: 'Turn this data into a live dashboard' }, tag: { zh: 'd3-visualization', en: 'd3-visualization' }, brand: true, ask: true },
    { id: { zh: '产出 · 交互式', en: 'Output · interactive' }, title: { zh: '数据看板.html', en: 'dashboard.html' }, tag: { zh: '.html', en: '.html' } },
  ],
  [
    { id: { zh: '排版完成', en: 'Layout done' }, title: { zh: 'resume-modern · 已生成', en: 'resume-modern · generated' }, tag: { zh: '技能', en: 'Skill' } },
    { id: { zh: '产出 · 220 KB', en: 'Output · 220 KB' }, title: { zh: '简历-2026.pdf', en: 'resume-2026.pdf' }, tag: { zh: '.pdf', en: '.pdf' } },
    { id: { zh: '已接入 2 个系统', en: '2 systems linked' }, title: { zh: '连接器 · MCP', en: 'Connectors · MCP' }, tag: { zh: '连接器', en: 'Connector' } },
    { id: { zh: 'ENG · 会话 #15', en: 'ENG · Session #15' }, title: { zh: '给发布会写三条社交卡片', en: 'Write three social cards for launch' }, tag: { zh: 'card-twitter', en: 'card-twitter' }, brand: true, ask: true },
    { id: { zh: '已完成', en: 'Done' }, title: { zh: 'card-twitter · 卡片已生成', en: 'card-twitter · cards generated' }, tag: { zh: '技能', en: 'Skill' } },
  ],
]
```

**若 Step 1 发现现有类型不叫 `Copy`**，把上面所有 `Copy` 换成实际的类型名，别新增类型。

- [ ] **Step 3: typecheck——此时预期会失败**

```bash
bun run typecheck
```

Expected: **报错**，形如 `Property 'icon' does not exist on type 'WallCard'` 或 `Property 'text' does not exist`，位置在 `components/HeroWall.tsx:84-85`。

**这就是本 Task 的失败的测试**：旧的 `HeroWall` 还在读 `card.icon` / `card.text`，而新结构没有这两个字段。Task 4 重写 `HeroWall` 后消失。

- [ ] **Step 4: 让 build 暂时能过——最小改动**

为了本 Task 能独立提交，把 `components/HeroWall.tsx:82-87` 的卡片渲染临时改成读新字段：

```tsx
<span
  key={ci}
  className="flex items-center gap-3 rounded-2xl border border-edge bg-panel/70 px-7 py-5 font-mono text-[15px] whitespace-nowrap text-dim"
>
  <span className={card.brand ? 'text-brand' : 'opacity-70'}>{t(card.tag)}</span>
  <span className={card.ask ? 'text-ink' : undefined}>{t(card.title)}</span>
</span>
```

同时把 `:61` 的 `opacity: 'var(--wall-opacity)'` 改成 `opacity: 0.5`（Task 1 删了那个令牌）。

**这是脚手架，Task 4 会整个推翻它。** 目的只是让每个 Task 都能独立通过构建、独立提交——而不是留一个build 不过的中间态给下一个人。

- [ ] **Step 5: typecheck + build**

```bash
bun run typecheck && bun run build
```

Expected: 都通过。

- [ ] **Step 6: 提交**

```bash
git add -A lib/content.ts components/HeroWall.tsx
git commit -m "feat: heroWall 数据结构升级为结构化卡片

从单行 chip（icon + text）改成有结构的卡片（id / title / tag / brand / ask）。
Linear intake 铺的是真实 UI 卡片，不是标签——数据结构得先跟上。
每行保留一张 ask 卡作为叙事锚点。HeroWall 的渲染暂时用脚手架顶着。"
```

---

## Task 4: 重写 HeroWall——从背景升格为主视觉

**Files:**
- Modify: `components/HeroWall.tsx`（全文重写）
- Modify: `app/globals.css`（`wallDrift` keyframes 保留/微调 + 新增 `breathe`）

**Interfaces:**
- Consumes: Task 3 的 `heroWall: WallCard[][]` 与 `WallCard` 字段名（`id` / `title` / `tag` / `brand` / `ask`）
- Produces: `<HeroWall />`——零 props，自带 `absolute inset-0`。Task 5 直接摆进 Hero 的 `absolute inset-0` 容器里。

**全部数值出自 spec §4，已是「存在感 47 / 遮罩 48」代入后的固化结果。别再改，也别留滑杆。**

- [ ] **Step 1: 确认 `wallDrift` keyframes 现状**

```bash
grep -n "wallDrift\|wall-row\|@keyframes breathe" app/globals.css
```

Expected: 找到 `wallDrift` 和 `.wall-row`（现有的），**找不到** `breathe`（Step 3 要加）。

- [ ] **Step 2: 全文重写 `components/HeroWall.tsx`**

```tsx
'use client'

/*
  Hero 的卡片墙——照 linear.app/intake 顶部的配方，换成我们自己的内容：
  一面向后倾倒的 3D 平面，五行「产品干活的真实痕迹」以不同速度漂移。

  2026-07-17 从「背景装饰」升格为「第一屏主视觉」（旧版被压到几乎看不见）。
  存在感的大头是 perspective:1000px + rotateX(42deg)——相机更近、卡片正面
  露得更多。这两个数比任何透明度调整都关键，别随手改。

  三层动：
  1. 每行自己的 CSS 无限漂移（wallDrift，不同 duration、奇数行反向）——永动氛围；
  2. 滚动视差：滚出首屏时整面墙比内容慢半拍上移——纵深感；
  3. 鼠标微视差（×0.012，很轻）——全站「跟手」哲学的延续。
  跟手值照例走「挂载后再接线」门闩（hydration 教训，踩过两次）。

  可读性纪律（原型实测撞出来的，别推翻）：
  - 墙是背景不是内容：aria-hidden + pointer-events:none；
  - 文案的可读性靠 Hero 里的毛玻璃层（模糊而非压暗），不靠把墙压暗——
    压暗会杀掉存在感，模糊不会。详见 spec §4.5。
*/

import { useEffect, useRef, useState } from 'react'
import { motion, useReducedMotion, useScroll, useSpring, useTransform } from 'motion/react'
import { heroWall } from '@/lib/content'
import { usePrefs } from '@/lib/prefs'
import { ensurePointerTracking, pointerX, pointerY } from './fx/pointer'

/*
  每行的漂移时长与方向，以及景深（存在感 47 的固化结果）。
  速度差是「墙在呼吸」的来源，别统一。焦点在第 3 行（index 2）：
  它最清晰、最亮，眼睛会先落在那儿。
*/
const ROWS = [
  { duration: 74, reverse: false, opacity: 0.3, blur: 4.1 },
  { duration: 92, reverse: true, opacity: 0.52, blur: 1.6 },
  { duration: 64, reverse: false, opacity: 0.85, blur: 0 },
  { duration: 100, reverse: true, opacity: 0.6, blur: 1.2 },
  { duration: 82, reverse: false, opacity: 0.26, blur: 5.2 },
]

export function HeroWall() {
  const { t } = usePrefs()
  const reduced = useReducedMotion()
  const ref = useRef<HTMLDivElement>(null)
  const [mounted, setMounted] = useState(false)
  useEffect(() => {
    setMounted(true)
    if (!reduced) ensurePointerTracking()
  }, [reduced])

  // 滚动视差：首屏滚出过程中墙上移 60px（比内容慢 = 在更远处）
  const { scrollYProgress } = useScroll({ target: ref, offset: ['start start', 'end start'] })
  const parallaxY = useTransform(scrollYProgress, [0, 1], [0, -60])
  // 鼠标微视差
  const mx = useSpring(useTransform(pointerX, (v) => (v - (typeof window === 'undefined' ? 0 : innerWidth / 2)) * 0.012), { stiffness: 40, damping: 18 })
  const my = useSpring(useTransform(pointerY, (v) => (v - (typeof window === 'undefined' ? 0 : innerHeight / 2)) * 0.012), { stiffness: 40, damping: 18 })

  const still = !mounted || reduced

  return (
    <div ref={ref} aria-hidden="true" className="pointer-events-none absolute inset-0 overflow-hidden">
      {/* 3D 舞台：透视点偏上，平面向后倒 */}
      <div className="absolute inset-0" style={{ perspective: '1000px', perspectiveOrigin: '50% 30%' }}>
        <motion.div
          className="absolute top-[-34%] left-[-30%] flex h-[176%] w-[176%] flex-col justify-between gap-[30px]"
          style={{
            transform: 'rotateX(42deg) rotateZ(-8deg)',
            transformStyle: 'preserve-3d',
            x: still ? 0 : mx,
            y: still ? 0 : my,
            translateY: still ? 0 : parallaxY,
          }}
        >
          {heroWall.map((row, ri) => {
            const conf = ROWS[ri % ROWS.length]
            return (
              <div
                key={ri}
                className="wall-row flex w-max gap-[22px] will-change-transform max-lg:gap-4"
                style={{
                  animationDuration: `${conf.duration}s`,
                  animationDirection: conf.reverse ? 'reverse' : 'normal',
                  animationPlayState: reduced ? 'paused' : 'running',
                  opacity: conf.opacity,
                  filter: conf.blur ? `blur(${conf.blur}px)` : undefined,
                }}
              >
                {/* 内容渲染两遍：位移到 -50% 时首尾无缝衔接 */}
                {[...row, ...row].map((card, ci) => (
                  <article
                    key={ci}
                    className={`w-[290px] flex-none rounded-[14px] border px-[18px] py-4 max-lg:w-[200px] max-lg:px-3 max-lg:py-3 ${
                      card.ask ? 'border-edge-brand' : 'border-edge bg-panel'
                    }`}
                    style={{
                      boxShadow: 'var(--shadow-card)',
                      // ask 卡（用户说的话）自带一层品牌绿——它是这面墙的叙事锚点
                      background: card.ask
                        ? 'linear-gradient(145deg, rgba(74,222,128,.06), var(--panel))'
                        : undefined,
                    }}
                  >
                    <div className="mb-[9px] flex items-center justify-between font-mono text-[10.5px] text-dim opacity-75 max-lg:text-[9px]">
                      <span className="truncate">{t(card.id)}</span>
                      <span className="tracking-[1px] opacity-60">···</span>
                    </div>
                    <div
                      className={`truncate text-[14.5px] leading-[1.45] max-lg:text-[12px] ${
                        card.ask ? 'font-medium text-ink' : 'text-[#c3d2c9]'
                      }`}
                    >
                      {t(card.title)}
                    </div>
                    <div className="mt-[13px] flex items-center gap-[7px] max-lg:mt-2">
                      <span
                        className={`flex items-center gap-[5px] rounded-md border px-[9px] py-[3px] font-mono text-[10.5px] max-lg:text-[9px] ${
                          card.brand ? 'border-edge-brand text-brand' : 'border-edge text-dim'
                        }`}
                      >
                        <i className={`size-[5px] rounded-full ${card.brand ? 'bg-brand' : 'bg-dim'}`} />
                        {t(card.tag)}
                      </span>
                      <span className="ml-auto size-5 rounded-full bg-gradient-to-br from-brand to-teal opacity-50" />
                    </div>
                  </article>
                ))}
              </div>
            )
          })}
        </motion.div>
      </div>

      {/* 核心光晕：Orbit 唯一被继承下来的东西——「所有产出都出自同一个核心」
          这个信息不能白丢。现在它是墙背后透出来的一团光，不是一个转圈的装置。 */}
      <div
        className="pulse-core absolute z-[1] size-[620px] -translate-x-1/2 -translate-y-1/2 max-lg:hidden"
        style={{
          top: '44%',
          left: '56%',
          background: 'radial-gradient(circle, rgba(74,222,128,.13), transparent 65%)',
          animationPlayState: reduced ? 'paused' : 'running',
        }}
      />
    </div>
  )
}
```

- [ ] **Step 3: `app/globals.css` 加 `breathe` keyframes**

在 `wallDrift` 附近加：

```css
/* 核心光晕的呼吸（HeroWall）。慢，6 秒一轮——快了就成了闪，会抢眼。 */
@keyframes breathe {
  0%,
  100% {
    opacity: 0.55;
    transform: translate(-50%, -50%) scale(1);
  }
  50% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.1);
  }
}
.pulse-core {
  animation: breathe 6s ease-in-out infinite;
}

/* 减少动效偏好：墙静止但仍可见——不是把它藏起来，是让它别动。 */
@media (prefers-reduced-motion: reduce) {
  .wall-row,
  .pulse-core {
    animation: none;
  }
}
```

确认 `.wall-row` 的 `wallDrift` 动画仍在（`transform: translateX(0) → translateX(-50%)`），**不要改它**。

- [ ] **Step 4: typecheck + build**

```bash
bun run typecheck && bun run build
```

Expected: 都通过（Task 3 Step 3 的类型报错到这里消失——脚手架被真实现取代）。

- [ ] **Step 5: 截图验证——墙本身对不对**

```bash
bun run dev &
sleep 4
cd /private/tmp/claude-501/-Users-kika-Desktop-project-Electron-claude-desktop-website/9014c5d1-5a6f-4f78-86c9-99e53f1efb43/scratchpad
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --screenshot=t4-wall.png --window-size=1440,900 --virtual-time-budget=3000 \
  --user-data-dir=/tmp/cdp-t4 http://localhost:3000
```

Expected: 打开 `t4-wall.png` 逐条核对——
- 卡片是**有结构的**（顶行小字 + 主标题 + 底部标签 + 头像圆点），不是单行 chip；
- 中间那行明显最清晰最亮，上下越远越糊；
- 有绿色 ask 卡散布在各行；
- 中间偏右透出一团绿光。

**此时文案会被墙压得难读——这是预期的**，毛玻璃在 Task 5 才加。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: HeroWall 重写——从背景装饰升格为第一屏主视觉

perspective 1100→1000px、倾角 38→42°：相机更近、卡片正面露得更多，
这是存在感的大头。卡片改为结构化渲染（顶行/主行/标签/头像）。
每行独立的 opacity+blur 做景深，焦点在第 3 行。
数值全部出自 spec §4（存在感 47 实拉校准后的固化值）。
Orbit 的「所有产出出自同一核心」由呼吸光晕继承。"
```

---

## Task 5: Hero 重排 + 毛玻璃 + Terminal 移出

**Files:**
- Modify: `components/sections/Hero.tsx`（双栏 → 单栏、加 glass/veil、标题手动断行、移出 Terminal）
- Modify: `app/page.tsx`（Terminal 升为独立 section）
- Modify: `lib/content.ts`（`hero.headline` 改为两行断法）

**Interfaces:**
- Consumes: Task 4 的 `<HeroWall />`（零 props）
- Produces: Hero 为单栏版面；`<Terminal />` 由 `app/page.tsx` 直接渲染，不再是 Hero 的子节点。

**层级纪律（spec §4.5，必须显式写 z-index，不许靠 DOM 顺序决胜）：**
`stage(wall)` z0 → `core` z1 → `glass` z2 → `veil1`/`veil2` z2（在 glass 之后）→ 文案 z3。
**`glass` 必须在两层 veil 之下：它模糊的是墙，不是遮罩。**

- [ ] **Step 1: 确认 headline 现状**

```bash
grep -n "headline\|accentLine" lib/content.ts | head
```

Expected: 找到 `headline: { zh: [...], en: [...] }`（多行数组）和 `accentLine`。

- [ ] **Step 2: `lib/content.ts` 改标题断行**

铺满版标题改两行——**手动定死断行，不靠宽度限制**（中文断在词中间很难看，原型实测过「做/完 PPT」「方/案」）：

```ts
headline: {
  zh: ['一句话，让 AI 帮你做完', 'PPT、表格、方案。'],
  en: ['One sentence. Claude ships', 'the deck, the sheet, the plan.'],
},
accentLine: 1,
```

- [ ] **Step 3: `components/sections/Hero.tsx` 重排**

改动点（其余原样保留）：

1. `<section>` 的 className 改成（去掉 max-w 容器，让墙铺满）：

```tsx
<section id="top" className="relative z-[1] min-h-screen overflow-hidden">
```

2. 墙 + 三层遮罩，放在 section 开头：

```tsx
{/* 墙（z0）+ 核心光（z1，在 HeroWall 内部） */}
<HeroWall />

{/* glass（z2）：文字栏背后的毛玻璃。
    压暗会杀掉墙的存在感，模糊不会——卡片的轮廓、动感、绿光全留着，
    只把它的字揉糊，就不跟副标题抢眼睛了。这是可读性的主力，不是装饰。
    边缘必须用 mask 化开：一旦让人看出这里有块方玻璃，整个效果就塌了。
    数值出自 spec §4.5（遮罩 48 的固化结果）。 */}
<div
  aria-hidden="true"
  className="pointer-events-none absolute z-[2] max-lg:hidden"
  style={{
    left: '-4%',
    top: '30%',
    width: '62%',
    height: '56%',
    backdropFilter: 'blur(15.3px) saturate(.85)',
    WebkitBackdropFilter: 'blur(15.3px) saturate(.85)',
    maskImage: 'radial-gradient(ellipse 54% 54% at 34% 50%, #000 42%, transparent 80%)',
    WebkitMaskImage: 'radial-gradient(ellipse 54% 54% at 34% 50%, #000 42%, transparent 80%)',
  }}
/>

{/* veil1（z2）：上下压边，保住导航和滚动提示 */}
<div
  aria-hidden="true"
  className="pointer-events-none absolute inset-0 z-[2]"
  style={{
    opacity: 0.766,
    background:
      'linear-gradient(180deg, var(--canvas) 0%, transparent 32%, transparent 56%, rgba(7,11,9,.88) 90%, var(--canvas) 100%)',
  }}
/>

{/* veil2（z2）：文字栏的保命符。重心压在副标题+按钮那一带（y58%）
    而不是标题——大标题字大字粗，自己扛得住，不需要保护。
    移动端没有 glass，靠它加强到 0.85 顶上（spec §8）。 */}
<div
  aria-hidden="true"
  className="pointer-events-none absolute inset-0 z-[2] opacity-[0.48] max-lg:opacity-[0.85]"
  style={{
    background:
      'radial-gradient(ellipse 46% 42% at 27% 58%, var(--canvas) 30%, rgba(7,11,9,.72) 62%, transparent 84%)',
  }}
/>
```

3. 文案容器改单栏（z3）：

```tsx
<div className="relative z-[3] mx-auto grid min-h-screen max-w-[1180px] content-center px-8 pt-[120px] pb-10">
  <div>
    {/* badge / h1 / 副标题 / 按钮 / 信任条：原样保留，一行都不用改 */}
  </div>
</div>
```

原来的 `<div className="relative grid items-center gap-12 lg:grid-cols-[1.08fr_1fr]">` 双栏容器**删掉**（右栏已经空了）。

4. `<h1>` 的字号改成：`text-[clamp(2.4rem,4.4vw,3.9rem)]`（铺满版标题更宽，得收一档）。逐字动画逻辑**原样保留**。

5. **删掉** `<motion.div {...rise(4)} className="mt-16"><Terminal /></motion.div>` 整块，以及 `Terminal` 的 import。

6. 底部 `SCROLL` 提示加 `z-[3]`（别被遮罩盖住）。

**不许动**：`useRelease` / `getPlatformCards` / `guessPlatform` / `primaryHref` / `primaryLabel` 那整套三态逻辑，以及 `rise()` 和逐字动画。

- [ ] **Step 4: `app/page.tsx` 让 Terminal 独立成节**

在 `<Hero />` 之后插入 Terminal 独立一节：

```tsx
<section className="relative z-[1] mx-auto max-w-[1180px] px-8 py-24">
  <Terminal />
</section>
```

并在文件顶部 `import { Terminal } from '@/components/Terminal'`。

- [ ] **Step 5: typecheck + build**

```bash
bun run typecheck && bun run build
```

Expected: 都通过。

- [ ] **Step 6: 截图验证——这是全计划最关键的一次验证**

```bash
bun run dev &
sleep 4
cd /private/tmp/claude-501/-Users-kika-Desktop-project-Electron-claude-desktop-website/9014c5d1-5a6f-4f78-86c9-99e53f1efb43/scratchpad
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --screenshot=t5-hero.png --window-size=1440,900 --virtual-time-budget=3000 \
  --user-data-dir=/tmp/cdp-t5 http://localhost:3000
```

Expected: 打开 `t5-hero.png`，逐条核对（**对照 spec §10 验收清单**）——
- 大标题两行，断在「做完」之后，**没有**断成「做/完 PPT」；
- **副标题「画布和你自己的知识库——聊天，然后收文件。」背后干净，没有卡片文字穿过去**（这是原型里实测翻过车的地方，重点看）；
- 按钮区、信任条同样干净；
- 右半边的墙**没有**跟着变暗，卡片该看清的还看得清；
- 看不出文字背后有块「方形」的玻璃，边缘是化开的；
- Terminal 不在第一屏，滚下去才有。

**对照基准**：`scratchpad/shot-v3.png` 就是定稿的原型截图，长得应当基本一致。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: Hero 改为铺满式版面 + 毛玻璃可读性层

双栏→单栏，墙铺满当底、文案压在左侧。标题手动断行（中文靠宽度
限制断字会断在词中间）。Terminal 移到第二屏独立成节。

可读性用毛玻璃模糊而非压暗——存在感和可读性因此不再互斥。
veil2 重心压在副标题+按钮那一带，大标题自己扛得住不需要保护。
层级显式写 z-index，不靠 DOM 顺序决胜。"
```

---

## Task 6: 移动端减档

**Files:**
- Modify: `components/HeroWall.tsx`（3 行 + 降速）

**Interfaces:**
- Consumes: Task 4 的 `HeroWall` 与 `ROWS` 常量
- Produces: 无新接口。

Task 4/5 已经用 `max-lg:` 前缀处理了卡片尺寸、字号、`glass` 隐藏、`veil2` 加强、核心光隐藏。本 Task 只补两件 CSS 做不到的事：**行数**和**速度**。

- [ ] **Step 1: 加视口探测（走 hydration 门闩）**

在 `HeroWall` 的 `useEffect` 里，`setMounted(true)` 之后加：

```tsx
const [narrow, setNarrow] = useState(false)
useEffect(() => {
  setMounted(true)
  if (!reduced) ensurePointerTracking()

  // 移动端减档：行数和速度是 CSS 做不到的，只能在 JS 里判。
  // 必须在 mounted 之后——服务器上没有 matchMedia，首帧对不上会 hydration 报错。
  const mq = window.matchMedia('(max-width: 1023px)')
  const sync = () => setNarrow(mq.matches)
  sync()
  mq.addEventListener('change', sync)
  return () => mq.removeEventListener('change', sync)
}, [reduced])
```

**注意**：`narrow` 初值必须是 `false`（桌面态）——它得和服务器渲染出来的 HTML 一致。

- [ ] **Step 2: 按 narrow 减档**

把 `heroWall.map(...)` 那行改成：

```tsx
{/* 移动端只留中间 3 行（保住焦点行）——小屏上五行挤成一团，
    而且每行都在跑动画，白耗电。 */}
{(narrow ? heroWall.slice(1, 4) : heroWall).map((row, ri) => {
  const conf = (narrow ? ROWS.slice(1, 4) : ROWS)[ri]
  return (
```

并把 `animationDuration` 改成：

```tsx
// 小屏上同样的速度显得更快（视野窄，卡片划过得更频繁）——放慢 30%
animationDuration: `${conf.duration * (narrow ? 1.3 : 1)}s`,
```

- [ ] **Step 3: typecheck + build**

```bash
bun run typecheck && bun run build
```

Expected: 都通过。

- [ ] **Step 4: 截图验证——手机宽度**

```bash
bun run dev &
sleep 4
cd /private/tmp/claude-501/-Users-kika-Desktop-project-Electron-claude-desktop-website/9014c5d1-5a6f-4f78-86c9-99e53f1efb43/scratchpad
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --screenshot=t6-mobile.png --window-size=390,844 --virtual-time-budget=3000 \
  --user-data-dir=/tmp/cdp-t6 http://localhost:3000
```

Expected: 打开 `t6-mobile.png`——
- 墙只有 **3 行**，卡片明显更小；
- 标题、副标题、按钮全部可读（靠加强的 veil2，**没有**毛玻璃）；
- **没有横向滚动条**（这是最容易翻车的：墙宽 176% 又 rotateZ，若 `overflow-hidden` 没生效就会撑出滚动条）。

- [ ] **Step 5: 提交**

```bash
git add -A components/HeroWall.tsx
git commit -m "feat: HeroWall 移动端减档

小屏只留中间 3 行（保住焦点行）、漂移放慢 30%（视野窄，同速显得更快）。
卡片尺寸/字号/毛玻璃隐藏/veil 加强由 max-lg: 前缀处理。
matchMedia 走 hydration 门闩，初值取桌面态与 SSR 首帧对齐。"
```

---

## Task 7: 性能实测与降级验收

**Files:**
- Modify: `components/sections/Hero.tsx`（**仅在实测掉帧时**改）

**Interfaces:**
- Consumes: Task 5 的 glass 层
- Produces: 无。这是验收 Task，理想结果是**什么都不用改**。

本 Task 兑现 spec §7：毛玻璃是本次改版唯一的性能赌注，必须实测，**不许靠感觉宣称流畅**。

- [ ] **Step 1: 跑 Lighthouse 拿客观数字**

```bash
bun run build && bun run start &
sleep 5
cd /private/tmp/claude-501/-Users-kika-Desktop-project-Electron-claude-desktop-website/9014c5d1-5a6f-4f78-86c9-99e53f1efb43/scratchpad
bunx lighthouse http://localhost:3000 --only-categories=performance \
  --chrome-flags="--headless" --output=json --output-path=./lh.json --quiet
cat lh.json | grep -o '"score":[0-9.]*' | head -1
```

Expected: performance score ≥ 0.85。
**这是生产构建（`bun run start`）不是 dev**——dev 模式带热更新和 source map，测出来的数字没有意义。

- [ ] **Step 2: 人眼验帧率**

在真实浏览器打开 `http://localhost:3000`，DevTools → Performance → 录 5 秒，看 FPS 曲线。

Expected: 稳定 60fps，无长时间掉到 30 以下。

**若掉帧 → 执行 Step 3 降级；若不掉帧 → 跳到 Step 4。**

- [ ] **Step 3: 降级（仅在掉帧时执行）**

按 spec §7：关掉 glass，veil2 提到 0.85。改 `Hero.tsx`——

删掉整个 glass 那个 `<div>`，并把 veil2 的 className 改成：

```tsx
className="pointer-events-none absolute inset-0 z-[2] opacity-[0.85]"
```

（即桌面和移动端都用 0.85，`max-lg:` 前缀不再需要。）

在 Hero 顶部注释里记一笔：

```tsx
/* 毛玻璃方案实测掉帧，已降级为压暗遮罩（spec §7 的备胎路径）。
   代价：墙的存在感打折。若将来换了更快的实现，回滚这一步即可。 */
```

**原则**：丑一点、但字照读、墙照动。有备胎的效果才敢上。

- [ ] **Step 4: 跑完整验收清单**

对照 spec §10 逐条验，全部打勾：

```bash
cd /Users/kika/Desktop/project/Electron/claude-desktop-website
echo "── 死代码残留（应当无输出）──"
grep -rn "toggleTheme\|themeInitScript\|wall-opacity\|orbitOuter\|sat-outer\|<Orbit" app components lib
echo "── useRelease 完好 ──"
grep -c "inflight" lib/useRelease.ts
echo "── 类型与构建 ──"
bun run typecheck && bun run build
```

Expected: 第一条无输出；第二条 `3`；typecheck 无输出；build 成功。

手工验（截图或真机）：
- [ ] 深色 1440 宽：大标题、副标题、按钮、信任条全部可读，无卡片文字穿过
- [ ] `prefers-reduced-motion: reduce` 下墙完全静止（Chrome DevTools → Rendering → Emulate CSS media feature）
- [ ] 移动端 390 宽：无横向滚动条，墙 3 行正常
- [ ] 键盘 Tab 走一遍：焦点不会掉进墙里
- [ ] 下载按钮三态与改版前一致（loading 态可断网模拟）
- [ ] Terminal 在第二屏，功能不变
- [ ] Nav 上没有主题切换按钮，无空洞

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "perf: Hero 卡片墙性能实测验收

Lighthouse 生产构建实测 + 帧率人眼验证，spec §10 验收清单逐条走完。"
```

---

## 计划自查

**1. Spec 覆盖**

| Spec 章节 | 对应 Task |
|---|---|
| §2 定稿方案（铺满） | Task 5 |
| §3 组件结构 | Task 1–5（逐文件对上） |
| §4.1 舞台与墙 | Task 4 Step 2 |
| §4.2 五行景深 | Task 4 Step 2（`ROWS` 常量） |
| §4.3 卡片 | Task 3（数据）+ Task 4 Step 2（渲染） |
| §4.4 核心光晕 | Task 4 Step 2–3 |
| §4.5 可读性三层 | Task 5 Step 3 |
| §5 数据结构 | Task 3 |
| §6 回归清单 | Task 7 Step 4 |
| §7 性能与降级 | Task 7 |
| §8 移动端 | Task 6（+ Task 4/5 的 `max-lg:`） |
| §9 浅色下线 | Task 1 |
| §10 验收清单 | Task 7 Step 4 |

无遗漏。

**2. 占位符扫描**：无 TBD/TODO；每个改代码的步骤都给了完整代码；每条命令都给了预期输出。

**3. 类型一致性**：
- `WallCard` 拼写全程一致；字段名 `id`/`title`/`tag`/`brand`/`ask` 在 Task 3 定义、Task 4 消费，对得上。
- `usePrefs()` 在 Task 1 收窄为 `{ lang, setLang, t }`，后续 Task 只用 `t`，无人再碰 `theme`。
- `ROWS` 在 Task 4 定义（含 `duration`/`reverse`/`opacity`/`blur` 四个字段），Task 6 用 `.slice(1,4)` 复用，字段名一致。
- Task 3 Step 2 的类型引用 `Copy`，并在 Step 1 要求先核实真实类型名——若项目里不叫 `Copy` 则整体替换，不新增类型。

**4. 行号已核实**（2026-07-17 实测）：`Nav.tsx` 按钮 43-49、图标 82-89/91-96；`globals.css` `.light` 61-80、`--wall-opacity` 整块 180-185、`wallDrift` 175-179（保留）。
行号会随改动漂移——**执行时以 grep 到的实际内容为准，行号只是路标**。

**5. 关于 TDD 的偏离（明示）**：本项目无测试框架，且本次不引入——为「墙倾斜 42 度好不好看」写自动化断言，能保证行数是 5，却完全无法保证它好看或流畅，而后者才是这次改动的全部意义。替代验证：删除类任务用 `grep` 断言（严格，机器说了算，见 Task 1 Step 1↔8 的「先记录基线、改完必须一无所获」对照），视觉类任务用无头截图对照定稿原型 `shot-v3.png`，性能用 Lighthouse 生产构建实测。
